# react-native-touchables-bug

### Reproduce:

Clone the repo `git clone https://github.com/YassinEldeeb/react-native-touchables-bug`

`yarn` in your terminal to install the dependencies

`yarn start` to start the app
